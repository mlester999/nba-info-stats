import NBA from "nba";

const curry = NBA.findPlayer("Stephen Curry");

// const spotlightPlayer = async () => {
//   const playerInfo = await NBA.stats
//     .playerInfo({ PlayerID: curry.playerId })
//     .then((res) => res);

//   console.log(playerInfo);
// };

// spotlightPlayer();
